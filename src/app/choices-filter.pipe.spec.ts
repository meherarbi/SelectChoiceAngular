import { ChoicesFilterPipe } from './choices-filter.pipe';

describe('ChoicesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChoicesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
